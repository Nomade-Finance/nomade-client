import Register from './register';

export const metadata = {
  title: "Nomade Finance | Inscription",
  description: "Inscription",
};

export default function page() {
  return (
    <div>
      <Register/>
    </div>
  );
}